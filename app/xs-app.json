{
	"welcomeFile": "/index.html",
	"authenticationMethod": "route",
	"routes": [
		{
			"source": "^/controller/(.*)",
			"target": "/controller/$1",
			"destination": "cap-backend",
			"csrfProtection": true,
			"authenticationType": "xsuaa"
		},
		{
			"source": "/(.*)",
			"localDir": "resources",
			"authenticationType": "xsuaa"
		}
	]
}